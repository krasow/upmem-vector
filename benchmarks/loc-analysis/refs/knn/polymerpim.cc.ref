RED_T run_knn(const std::vector<DPUVector<T> >& da,
              const std::vector<T>& query) {
    auto dist = sqr(da[0] - query[0]);
    for (uint32_t d = 1; d < DIM; d++) {
        dist = dist + sqr(da[d] - query[d]);
    }
    auto pending = minimum(dist);
    return pending.get();
}
